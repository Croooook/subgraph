type IncentiveCreated @entity {
  id : ID!
  rewardToken : Bytes!
  pool : Bytes!
  startTime : BigInt!
  endTime : BigInt!
  refundee : Bytes!
  reward : BigInt!
  stakedTokenId : [TokenStaked!]! @derivedFrom (field : "incentiveId")
  status : [IncentiveEnded!]! @derivedFrom (field: "incentiveId")
} 

type IncentiveEnded @entity {
  id : ID!
  incentiveId : IncentiveCreated
  refund : BigInt!
}

type TokenStaked @entity{
  id : ID!
  tokenId : BigInt!
  incentiveId : IncentiveCreated
  liquidity : BigInt !
}

type TokenUnstaked @entity{
  id : ID!
  tokenId : BigInt!
  incentiveId : Bytes!

}

type DepositTransferred @entity {
  id: ID!
  tokenId: BigInt! 
  oldOwner: Bytes! 
  newOwner: Bytes! 
}